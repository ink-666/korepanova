# Лабораторная работа: Классы, наследование, обобщения и перегрузка методов в C#

**Студент:** Литвинов Семён    
**Предмет:** Разработка программных модулей

## 📋 Задание

Создать несколько классов (простой и производный) на произвольную тему, используя:
- Наследование классов
- Обобщённый метод (generic method)
- Перегрузку методов (method overloading)
- Подробные комментарии о передаче данных
- Обязательный вывод результата

## 🎯 Тема работы: Управление персоналом (Person и Employee)

В работе реализована модель "Человек-Сотрудник" с дополнительными возможностями:
- Базовый класс `Person` (человек)
- Производный класс `Employee` (сотрудник)
- Обобщённый класс `Department<T>` (отдел с идентификатором любого типа)
- Класс `Helper` с перегруженными методами и обобщённым методом

## 📊 Диаграмма классов

```
┌──────────┐
│ Object │
└──────────┘
↑
┌──────────┐
│ Person │
├──────────┤
│ • Name │
├──────────┤
│ • Print()│
└──────────┘
↑
┌──────────┐
│ Employee │
├──────────┤
│ • Company│
├──────────┤
│ • Print()│
└──────────┘
```

## 🔧 Реализованные концепции

### 1. Наследование классов
Класс `Employee` наследует класс `Person`. Через ключевое слово `base` вызывается конструктор родителя.

### 2. Обобщённый класс `Department<T>`
Позволяет создавать отделы с идентификатором любого типа (`int`, `string` и т.д.)

### 3. Обобщённый метод `Swap<T>`
Меняет местами две переменные любого типа без дублирования кода.

### 4. Перегрузка методов `Display()`
4 версии метода с разными параметрами (int, string, два int, double)

## 💻 Пример работы программы

--- ЧАСТЬ 1: Наследование классов ---

Создание объекта Person:
[КОНСТРУКТОР Person] Создан человек с именем: Семён
[МЕТОД Print] Person: Семён
Создание объекта Employee:
[КОНСТРУКТОР Person] Создан человек с именем: Иван
[КОНСТРУКТОР Employee] Сотрудник работает в: Microsoft
[МЕТОД Print] Employee: Иван, работает в Microsoft
...

## 📁 Структура проекта

CSharpProject/
├── Program.cs # Основной код программы
├── README.md # Этот файл с описанием
└── .gitignore # Игнорируемые файлы

## 🔍 Детальный разбор кода

### Базовый класс Person

```csharp
class Person
{
    public string Name { get; set; }
    
    public Person(string name)
    {
        Name = name;
        // Конструктор вызывается при создании любого Person или Employee
    }
}
```
### Производный класс Employee

```class Employee : Person
{
    public string Company { get; set; }
    
    public Employee(string name, string company) : base(name)
    {
        // base(name) - ПЕРЕДАЧА ДАННЫХ в конструктор Person
        Company = company;
    }
}
```

### Как передаются данные:

При создании new Employee("Иван", "Microsoft")
Строка "Иван" передаётся через base(name) в конструктор Person
Строка "Microsoft" сохраняется в свойстве Company
Порядок выполнения: сначала конструктор Person, потом Employee

### Обобщённый метод Swap

```public void Swap<T>(ref T first, ref T second)
{
    T temp = first;  // Сохраняем first во временную переменную
    first = second;  // Передаём second в first
    second = temp;   // Передаём temp (бывший first) в second
}
```

### ✅ Вывод

Работа демонстрирует все ключевые концепции ООП в C#:

✓ Наследование классов
✓ Обобщённые типы и методы
✓ Перегрузку методов
✓ Детальные комментарии о передаче данных
✓ Наглядный вывод результатов
